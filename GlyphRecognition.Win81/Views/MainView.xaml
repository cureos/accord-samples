<views:MvxWindowsPage
    x:Class="IPPrototyper.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:Cirrious.MvvmCross.WindowsCommon.Views"
    xmlns:converters="using:GlyphRecognition.Converters"
    mc:Ignorable="d">

    <views:MvxWindowsPage.Resources>
        <converters:BitmapToWriteableBitmapConverter x:Key="BitmapToWriteableBitmapConverter" />
    </views:MvxWindowsPage.Resources>
    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding RoutineName}" FontSize="40"/>
        <Button Grid.Row="1" Grid.Column="0" Command="{Binding LoadImagesCommand}" Content="Load images"/>
        <ListBox Grid.Row="2" Grid.Column="0" ItemsSource="{Binding OriginalImages}" SelectionMode="Single" 
                 SelectedItem="{Binding SelectedOriginalImage, Mode=TwoWay}" SelectionChanged="OriginalImagesOnSelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <Image Width="60" Source="{Binding Converter={StaticResource BitmapToWriteableBitmapConverter}}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Image Grid.Row="2" Grid.Column="1" Stretch="Uniform" 
               Source="{Binding DisplayedImage, Converter={StaticResource BitmapToWriteableBitmapConverter}}"/>
        <ListBox Grid.Row="3" Grid.Column="0" ItemsSource="{Binding ProcessedImages}" SelectionMode="Single" 
                 SelectedItem="{Binding SelectedProcessedImage, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Width="80" Source="{Binding Value, Converter={StaticResource BitmapToWriteableBitmapConverter}}"/>
                        <TextBlock Text="{Binding Key}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ScrollViewer Grid.Row="3" Grid.Column="1" VerticalScrollMode="Auto" HorizontalScrollMode="Auto" >
            <TextBlock Text="{Binding Log}"/>
        </ScrollViewer>
    </Grid>
</views:MvxWindowsPage>
